@model IEnumerable<BrotherGara.Models.THAMSO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GiaTri)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GiaTri)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenThamSo }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenThamSo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenThamSo })
        </td>
    </tr>
}

</table>
