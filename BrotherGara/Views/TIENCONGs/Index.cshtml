@model IEnumerable<BrotherGara.Models.TIENCONG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTienCong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TienCong1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTienCong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TienCong1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTienCong }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTienCong }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTienCong })
        </td>
    </tr>
}

</table>
